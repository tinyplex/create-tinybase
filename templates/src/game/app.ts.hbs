{{includeFile template="src/game/store.ts.hbs" output="src/store.ts"}}
import {store} from './store';
{{includeFile template="src/game/game.ts.hbs" output="src/game.{{ext}}"}}
import {createGame} from './game';

const app = () => {
const appContainer = document.getElementById('app')!;
appContainer.appendChild(createGame(store));
};

export {app};