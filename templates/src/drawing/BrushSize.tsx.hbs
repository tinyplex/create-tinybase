{{includeFile template="src/drawing/brushSize.css.hbs" output="src/brushSize.css"}}
import {useValue, useSetValueCallback} from './SettingsStore';
import './brushSize.css';

export const BrushSize = () => {
const size = (useValue('brushSize', 'settings') as number) ?? 5;

const setSize = useSetValueCallback(
'brushSize',
(e: React.ChangeEvent<HTMLInputElement>) => parseInt(e.target.value),
  [],
  'settings',
  );

  return (
  <div id="brushSize">
    <label>Size:</label>
    <input type="range" min="1" max="50" value={size} onChange={setSize} />
    <span id="brushSizeValue">{size}</span>
  </div>
  );
  };