import * as UiReact from 'tinybase/ui-react/with-schemas';

const TODOS_SCHEMA = {
todos: {
text: {type: 'string'},
completed: {type: 'boolean'},
},
} as const;

const UiReactWithSchemas = UiReact as UiReact.WithSchemas< [typeof TODOS_SCHEMA]>;

  export const {
  useAddRowCallback,
  useDelRowCallback,
  useRow,
  useRowIds,
  useSetPartialRowCallback,
  useStore,
  } = UiReactWithSchemas;

  export {TODOS_SCHEMA};