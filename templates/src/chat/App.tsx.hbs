{{includeFile template="src/chat/index.css.hbs" output="src/chat/index.css"}}
import './index.css';
import {StrictMode, useState, useMemo} from 'react';
import {createStore} from 'tinybase';
import {Provider, useCreateStore, useAddRowCallback, useRow, useSetValueCallback, useValue, useRowIds, useStore} from 'tinybase/ui-react';
import {Inspector} from 'tinybase/ui-react-inspector';
{{includeFile template="src/components/Button.tsx.hbs" output="src/components/Button.{{ext}}"}}
import {Button} from '../components/Button';
{{includeFile template="src/components/Input.tsx.hbs" output="src/components/Input.{{ext}}"}}
import {Input} from '../components/Input';
{{includeFile template="src/components/Message.tsx.hbs" output="src/components/Message.{{ext}}"}}
import {Message} from '../components/Message';
{{includeFile template="src/components/Messages.tsx.hbs" output="src/components/Messages.{{ext}}"}}
import {Messages} from '../components/Messages';
{{includeFile template="src/components/MessageInput.tsx.hbs" output="src/components/MessageInput.{{ext}}"}}
import {MessageInput} from '../components/MessageInput';
{{includeFile template="src/components/UsernameInput.tsx.hbs" output="src/components/UsernameInput.{{ext}}"}}
import {UsernameInput} from '../components/UsernameInput';

export const App = () => {
const store = useCreateStore(() => {
return createStore()
.setValue('username', 'Carol')
.setTable('messages', {
'1': {username: 'Alice', text: 'Hello!', timestamp: Date.now() - 60000},
'2': {username: 'Bob', text: 'Hi there!', timestamp: Date.now() - 30000},
});
});

return (
<StrictMode>
  <Provider store={store}>
    <UsernameInput />
    <Messages />
    <MessageInput />
    <Inspector />
  </Provider>
</StrictMode>
);
};