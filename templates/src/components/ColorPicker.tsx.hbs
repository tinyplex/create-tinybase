{{includeFile template="src/components/colorPicker.css.hbs" output="src/components/colorPicker.css"}}
import './colorPicker.css';
import {useValue, useSetValueCallback} from 'tinybase/ui-react';

export const ColorPicker = () => {
const colors = ['#d81b60', '#1976d2', '#388e3c', '#f57c00', '#7b1fa2', '#fff'];
const currentColor = useValue('brushColor', 'settings') as string;

const setColor = useSetValueCallback(
'brushColor',
(color: string) => color,
[],
'settings',
);

return (
<div id="colorPicker">
  {colors.map((color) => (
  <button key={color} className={`colorBtn${currentColor===color ? ' active' : '' }`} style=\{{background: color}} onClick={()=> setColor(color)}
    />
    ))}
</div>
);
};