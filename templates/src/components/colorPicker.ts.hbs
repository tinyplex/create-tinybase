{{includeFile template="src/components/colorPicker.css.hbs" output="src/components/colorPicker.css"}}
import './colorPicker.css';

export const createColorPicker = (store: any, containerId: string): void => {
const colors = ['#d81b60', '#1976d2', '#388e3c', '#f57c00', '#7b1fa2', '#fff'];
const container = document.getElementById(containerId)!;

colors.forEach((color) => {
const btn = document.createElement('button');
btn.className = 'colorBtn';
btn.style.background = color;
btn.addEventListener('click', () => {
const brush = store.getRow('settings', 'brush') as any;
store.setRow('settings', 'brush', {...brush, color});
});
container.appendChild(btn);
});
};